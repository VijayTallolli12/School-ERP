# Bug Report

| Severity | Bug | Evidence | Impact | Recommendation |
|---|---|---|---|---|
| Critical | `php artisan route:list` fails. | Command fails with `Target [App\Modules\Reports\Repositories\FeeDefaulterReportRepositoryInterface] is not instantiable`. `AppServiceProvider` binds the interface but lacks imports for `FeeDefaulterReportRepositoryInterface` and `FeeDefaulterReportRepository`. | Route cache/listing and production deployment checks can fail. Fee report route/controller resolution is unsafe. | Add the missing `use` statements or fully qualify the binding classes; add a route-list smoke test. |
| Critical | Database is not fully migrated. | `php artisan migrate:status` shows pending migrations: `2026_06_30_052653_make_trip_id_nullable_in_trip_events`, `2026_07_07_000001_create_hr_tables`, `2026_07_07_000002_create_exam_enhancement_tables`, `2026_07_07_000003_create_ai_query_logs_table`. | HR, exam enhancement, AI logging and trip event behavior are not live in current DB. | Run and validate pending migrations; add migration status to deployment checklist. |
| High | Live attendance realtime status test fails. | `php artisan test`: 101 passed, 1 failed. `Tests\Feature\LiveAttendanceTest > realtime status endpoint`; expected `data.student_attendance.total` to be 1 but got 0. | Realtime attendance dashboard/API can show incorrect counts. | Fix query/date/school scoping in realtime attendance status endpoint and keep regression test. |
| High | Debug mode enabled in local audited environment. | `php artisan about` reports Debug Mode ENABLED and Environment local. | Unsafe for production if deployed with same settings. | Require production `.env` validation in deploy preflight. |
| High | Reports module uses two view namespaces/locations. | Views exist in both `resources/views/modules/reports` and `app/Modules/Reports/Views`; routes include `routes/modules/reports.php` and `app/Modules/Reports/routes.php`, while `routes/modules/reports.php` is 69 bytes. | Increases risk of broken views and duplicated report ownership. | Consolidate report routes/views or clearly document namespace ownership. |
| Medium | Console debug statements in Blade. | `console.log` found in timetable, fees, and notifications Blade views. | Production console noise and potential exposure of operational state. | Remove debug logs or gate them behind local environment. |
| Medium | Large controllers and services. | PayrollController 688 lines, LibraryController 524, TransportController 496, FeesController 457, AttendanceController 280, ExamController 266. | Harder QA, higher regression risk. | Extract command/services by workflow and add focused tests. |
| Medium | Full test suite is slow. | 102 tests took 234 seconds. | Slows CI and discourages frequent regression runs. | Use in-memory DB where possible, seed only required fixtures, parallelize tests. |


<div class="d-flex align-items-center gap-2">
    <div>
        <div class="fw-semibold small">{{ $doc->student->full_name }}</div>
        <div class="small text-secondary">{{ $doc->student->admission_no }}</div>
    </div>
</div>
